# Handover — 2026-09-02, Track A Presentation Push

Read this first in a new session before touching any Track A presentation material.

## Project context (30-second version)

Building presentation materials on **Track A** (the adaptive k-mer cache thesis, stages S0–S5) for review, most recently with Kolin sir/Suthar in mind. Standing instructions that apply to **everything** below and to any future work on this material:

- Every stat must be framed as a **delta vs. the S0 baseline**, never a bare before/after number.
- Every experiment follows the same template: **Tried → Changed → Numbers/Graphs → Why it worked or didn't** — explicit small-DB-vs-big-DB comparison wherever the data supports it.
- **Never fabricate precise numbers** for anything the source only describes qualitatively (e.g. the two undocumented points in the S2 cache-size sweep are shown as honestly-labeled "flat, not individually reported," not invented values).
- Minimal, white-background, graphs-and-tables-first design — no dense prose walls, no decorative filler.

## Where things stand — everything shipped this session

**1. The interactive web deck — the one the user said looks best, now editable in-page:**
👉 **https://claude.ai/code/artifact/a85b108e-1f9e-4d59-9801-7c2596cee53c** ("Track A Debrief")

- 12 slides, S0 through the Scorecard, keyboard (← → / space) and click-zone navigation, matches `presentations/week3_deck.html`'s nav mechanism but a new minimal white aesthetic (that file's own "blueprint" visual style was rejected earlier and was deliberately NOT reused).
- Real inline SVG charts (bars, the S2 22× cliff line, the S4 diverging reversal) — no library, hand-rolled chart engine in the page's own `<script>`.
- **In-page editing is live**: click the ✎ pencil top-left → Edit → click any headline/dek/why-box/scorecard text → type → Save. Save calls the `artifact` runtime capability (`claude.use("artifact")` → `.publish()`), which republishes the page itself — no separate CMS. Only prose/labels are editable this way, not chart data (editing chart values safely needs its own numeric UI, out of scope).
- **Print-to-PDF is built in**: open the link, `Ctrl+P` → Save as PDF → each slide prints as its own page, identical to on-screen. This is the fast path if a static PDF copy is ever needed — no separate export tooling required.
- ⚠️ **Important for next session**: because the page can now save new versions of itself, the **live published artifact is the source of truth, not the local scratchpad file**. The local copy is at `C:\Users\user\AppData\Local\Temp\claude\...\78be3688-1f66-463a-a3d6-00e4091af8ad\scratchpad\track_a_webdeck.html`, but that temp directory is session-scoped and may not even exist in a new session. **Before editing this deck again, always `Artifact` → `action: "read"` on the URL above first** — someone may have edited text directly on the page since. (This already happened once this session: a caption got accidentally cleared via the Edit button and had to be restored — see the merge-conflict handling in this session's transcript if useful precedent.)

**2. The Canva deck — user says they'll take it from here:**
- View: https://www.canva.com/d/uyNzwSsapE5Oxu9
- Edit: https://www.canva.com/d/4nK5V2pTsruGLak
- Design ID `DAHT-NmFbiI`, 14 pages, white backgrounds throughout, three real bar charts built with Canva's `insert_shape` primitive (no native chart tool exists there — a unit-square path scaled via width/height is the only way to draw a proportional bar): page 3 (S0 baseline LLC-miss%), page 6 (S2 cache-size cliff, including the honest tiny "flat" bars), page 11 (S4 diverging reversal, the centerpiece).
- **User is now updating this themselves.**

**3. Markdown decks + their .pptx exports — all committed and pushed to GitHub, `presentations/`:**
| Doc | Purpose |
|---|---|
| [`track_a_1pager_2026-09-02.md`](https://github.com/KathpaliaChirag/Nanopore-project/blob/main/presentations/track_a_1pager_2026-09-02.md) (+ `.pptx`) | Recap, 1-page depth |
| [`track_a_progress_2026-09-02.md`](https://github.com/KathpaliaChirag/Nanopore-project/blob/main/presentations/track_a_progress_2026-09-02.md) (+ `.pptx`) | Full briefing depth |
| [`track_a_appendix_2026-09-02.md`](https://github.com/KathpaliaChirag/Nanopore-project/blob/main/presentations/track_a_appendix_2026-09-02.md) (+ `.pptx`) | Data appendix |
| [`track_a_study_guide_2026-09-02.md`](https://github.com/KathpaliaChirag/Nanopore-project/blob/main/presentations/track_a_study_guide_2026-09-02.md) (+ `.pptx`, 59 slides) | Deep, explanatory, verbatim code from every `plan_paper/scripts/*.py` patch, glossary, master Q&A, commit ledger |

All four also have a **Scorecard** section ("what actually sped things up vs. what didn't") — the same content that's on web-deck slide 12 and Canva page 11's centerpiece.

## Scope for the next session — read this before doing anything

The user is now **building the PPT themselves** and **updating Canva themselves**. Do not default to generating more full decks (pptx, Canva outlines, HTML dashboards) unless explicitly re-asked — that was this session's job, it's done. What they said they'll come back for:

> "will take ur help in making graphs and explanation"

So the expected next-session role is narrower: **produce individual graphs/charts and explain specific numbers on request**, to be dropped into their own manually-built PPT/Canva — not full-deck authorship. If asked for a graph, match the established visual language (categorical blue `#2a78d6` / accent, good `#0ca30c` / critical `#d03b3b` / pending `#b8860b`, IBM Plex Mono for data/labels, Literata for headings — see the web deck's `<style>` block for the exact token set) so anything produced drops in visually consistent with what already exists.

## Secondary open thread (unresolved, tangential — not presentation-related)

User was trying to SSH into the Orion Jetson (`jetsonagx@10.154.233.173`, per `dorado-kraken-research/CLAUDE.md`) from their Windows laptop. Both laptop and Jetson confirmed on campus wifi, but the connection still timed out (not refused) — likely campus-wifi client isolation (blocks device-to-device traffic even on the same SSID) or a stale IP (the documented address may not match the Jetson's current DHCP-assigned wifi address). Left mid-diagnosis:
- Next step suggested but not yet run: `ping 10.154.233.173` from the laptop to isolate network-level block vs. SSH-specific issue.
- If the user has physical/local access to the Jetson, run `hostname -I` on the Jetson itself to get its real current IP.
- Not resolved — pick back up only if the user raises it again.

## File map

- Web deck: https://claude.ai/code/artifact/a85b108e-1f9e-4d59-9801-7c2596cee53c
- Track A Lab Report (earlier per-experiment chart artifact, superseded in spirit by the web deck but still live): https://claude.ai/code/artifact/0180ec50-7481-43f9-9ee3-171d22e14b57
- Canva: view https://www.canva.com/d/uyNzwSsapE5Oxu9 · edit https://www.canva.com/d/4nK5V2pTsruGLak
- [`presentations/`](https://github.com/KathpaliaChirag/Nanopore-project/tree/main/presentations) — all four md/pptx pairs above, plus this file
- [`plan_paper/command_log.md`](https://github.com/KathpaliaChirag/Nanopore-project/blob/main/plan_paper/command_log.md) — source of truth for every number used in the above (Track A S0–S5 real code/results)
