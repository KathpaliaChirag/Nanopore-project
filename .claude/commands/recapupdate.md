Read `dorado-kraken-research/CLAUDE_RECAP.md` (moved out of the project root on 2026-07-04 when the repo split into `dorado-kraken-research/` for prior Dorado/Kraken2 work and root-level Mamba/MHA work). Then check `dorado-kraken-research/docs/updates.md` for the most recent session entry (the last `##` section at the bottom). Check `dorado-kraken-research/AccuracyDrift/RESULTS.md` run checklist for any newly checked-off items. Then update `dorado-kraken-research/CLAUDE_RECAP.md` to reflect the current state of the project:

1. Update "Last updated" date to today.
2. Replace "What happened last" with a concise bullet summary of what actually changed this session (experiments run, files edited, decisions made). If nothing happened this session yet, keep the previous entry.
3. Update the "Pending" checklists — check off anything that got done, add anything new that came up.
4. Correct any key numbers or facts that changed.
5. Keep the file under ~120 lines total. Cut detail that's now stale rather than appending.

Do not add a session-log style chronological entry — CLAUDE_RECAP.md is a living snapshot, not a log. The log lives in `dorado-kraken-research/docs/updates.md`.

After updating, confirm in one sentence what changed in the recap.
