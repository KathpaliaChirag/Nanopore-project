# Raw sequencing data — never commit
pod5 data/
*.pod5
*.fast5

# Dorado outputs
*.bam
*.fastq
*.sam
*.cram

# Large model files
*.pt
*.onnx

# Dorado binary (installed separately)
dorado/

# Dorado temp files (created during model download + pod5 migration)
.temp_dorado_model-*/
.tmp_pod5_v3_v4_migration_*/

# Results (local only — too large for git)
results/

# Large binary downloads (install separately)
*.tar.gz
*.zip

# Old folder name (moved to resources/)
tools-guide-claude/

# Claude local settings
.claude/settings.local.json

# AMDuProf profiling session output
AMDuProf-*/

# gprof output
gmon.out
# NOTE: **/ prefix so these still match after the 2026-07-04 restructure into
# dorado-kraken-research/ (the old bare Luna/ paths went stale). Several of these
# files exceed GitHub's 100 MB limit and must never be committed.
**/Luna/profiling/perf_output_hac_full.txt
**/Luna/profiling/perf_output_sup.txt
**/Luna/profiling/perf_record_hac_32t_out.txt

# M1-M7 large binary/data files (keep only txt summaries)
**/Luna/profiling/pending/m5_histogram.txt
**/Luna/profiling/pending/*.data

# Raw per-read kraken2 output dumps — often exceed GitHub's 100MB limit,
# only report.txt/perf.txt summaries are needed
**/output.txt

# Office temp lock files
~$*.pptx
~$*.docx
~$*.xlsx
